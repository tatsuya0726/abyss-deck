const http=require('http'), fs=require('fs'), path=require('path');
const root=path.resolve(__dirname,'../dist');
const types={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.webp':'image/webp','.mp3':'audio/mpeg','.ogg':'audio/ogg','.svg':'image/svg+xml'};
http.createServer((req,res)=>{let p;try{p=path.resolve(root,'.'+decodeURIComponent(new URL(req.url,'http://localhost').pathname));}catch{res.writeHead(400).end();return;}if(p!==root&&!p.startsWith(root+path.sep)){res.writeHead(403).end();return;}if(p===root)p=path.join(root,'index.html');fs.stat(p,(err,s)=>{if(err||!s.isFile()){console.log('404',req.url);res.writeHead(404).end();return;}res.setHeader('Content-Type',types[path.extname(p)]||'application/octet-stream');res.setHeader('Cache-Control','no-store');res.setHeader('Accept-Ranges','bytes');let start=0,end=s.size-1;const m=/^bytes=(\d+)-(\d*)$/.exec(req.headers.range||'');if(m){start=+m[1];end=m[2]?Math.min(+m[2],end):end;if(start>end){res.writeHead(416).end();return;}res.statusCode=206;res.setHeader('Content-Range',`bytes ${start}-${end}/${s.size}`);}res.setHeader('Content-Length',end-start+1);if(req.method==='HEAD')res.end();else fs.createReadStream(p,{start,end}).pipe(res);});}).listen(8119,'127.0.0.1',()=>console.log('ABYSS DECK v119 Local: http://127.0.0.1:8119'));
